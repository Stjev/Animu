/**
 * This function is called by the div on the home screen
 * This will open the users folders to find a torrent file.
 */
function open_files(){
    $('#file-input').click(); // this will click the button that will open the file explorer
}